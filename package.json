{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt generate && swa start ./dist /api",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.12",
    "sass": "^1.55.0",
    "sass-loader": "^10.2.1",
    "vue-tsc": "^0.40.5",
    "@azure/static-web-apps-cli": "^1.0.2",
    "typescript": "^4.7.4"
  }
}
